<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker">

    <property name="charset" value="UTF-8"/>

    <!-- Taille maximale d'un fichier -->
    <module name="FileLength">
        <property name="max" value="500"/>
    </module>

    <!-- Longueur maximale d'une ligne (au niveau Checker) -->
    <module name="LineLength">
        <property name="max" value="120"/>
    </module>

    <module name="TreeWalker">

        <!-- Nommage des classes : commence par une majuscule -->
        <module name="TypeName"/>

        <!-- Nommage des méthodes : commence par une minuscule -->
        <module name="MethodName"/>

        <!-- Nommage des variables : camelCase -->
        <module name="LocalVariableName"/>

        <!-- Nommage des constantes : MAJUSCULES -->
        <module name="ConstantName"/>

        <!-- Pas d'imports inutilisés -->
        <module name="UnusedImports"/>

        <!-- Pas d'imports avec wildcard (ex: import java.util.*) -->
        <module name="AvoidStarImport"/>

        <!-- Accolades obligatoires pour if/for/while -->
        <module name="NeedBraces"/>

        <!-- Modificateurs dans le bon ordre (public static final...) -->
        <module name="ModifierOrder"/>

    </module>

</module>
